<pre lang="markdown"> ### 📁 Repository Structure ```text . ├── notebooks/ │ └── OpenFDA_RAGAs_v1.1.ipynb # Main notebook for running and evaluating RAG pipeline ├── demo.py # Entry-point script for testing the QA system ├── fda_queries_rows.csv # Collected medication-related queries (from app + literature) ├── OpenFda_Query.md # Technical notes or query format details ├── .env.example # Example environment variables (e.g., API keys) ├── .gitignore # Git ignore rules ├── .python-version # Python version used for the project ├── pyproject.toml # Project metadata and dependencies ├── uv.lock # Dependency lockfile (generated by uv or poetry) └── README.md # Project overview and usage instructions ``` </pre>
